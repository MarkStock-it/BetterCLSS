{
  "name": "betterclss",
  "version": "1.0.0",
  "private": true,
  "description": "BetterCLSS backend with Express + Firebase Cloud Messaging",
  "scripts": {
    "check": "node scripts/check.js",
    "dev": "node server.js",
    "test": "npm run check",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0"
  }
}
